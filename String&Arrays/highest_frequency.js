// write a function that takes an array of strings and return the most commonly occurring strings
// in that array

// if there are multiple strings with the same high frequency, 
// return the one that appears first in the array

// input: Array of Strings
// Output: String

Examples:
console.log(highestFrequency(['a','b','c','c','d','e']), // => c
highestFrequency(['abc','def','abc','def','abc']), // => abc
highestFrequency(['abc','abc','def','def','ghi','ghi','ghi','ghi','ghi']) // => c
)

function highestFrequency(str){
    for(let i =0; i < str.length; i++){

    }
}